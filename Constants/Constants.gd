class_name Constants
extends Node

enum PlayerColor {WHITE, BLACK, BLUE, RED}

enum MapCellType {
	GROUND = 0, 
	WALL = 1, 
	POWER_UP = 2,
	BRICK = 10
}

enum PowerUpType {EXTRA_BOMB, BLAST_INCREASE, SPEED_INCREASE}
